# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: openai-plugin-readyapi
metadata:
  template: openai-plugin-readyapi@0.0.1-beta
services:
  api:
    project: .
    host: containerapp
    language: python